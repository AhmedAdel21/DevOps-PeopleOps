export {
  departmentDtoToDomain,
  departmentDetailDtoToDomain,
} from './department.mapper';
// `mapHttpErrorToManagement` is shared — import it directly from
// `@/data/mappers/team_attendance` (not re-exported here to avoid a
// duplicate name in the `@/data/mappers` barrel).
