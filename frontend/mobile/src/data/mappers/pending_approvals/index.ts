export { pendingApprovalsPageDtoToDomain } from './pending_approvals.mapper';
// Management HTTP-error mapping is shared — import `mapHttpErrorToManagement`
// directly from `@/data/mappers/team_attendance` (NOT re-exported here, to
// avoid a duplicate name in the `@/data/mappers` barrel).
